<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.lenovo.dictionary">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.MOUNT_UNMOUNT_FILESYSTEMS" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="英语词典"
        android:theme="@style/AppTheme">
        <activity android:name=".activity.Start">
        </activity>
        <activity android:name=".activity.MainActivity1">
        </activity>
        <activity android:name=".activity.Start1">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
            </activity>
        <activity
            android:name=".activity.MainActivity"></activity>
        <activity
            android:name=".activity.MainActivity2"></activity>
        <activity
            android:name=".activity.MainActivity3"></activity>
    </application>

</manifest>

